# 服务器网络排障、固件升级与运维记录（脱敏版）

| 项 | 内容 |
| :--- | :--- |
| **适用范围** | 带独立管理控制器和业务控制面的加速服务器 |
| **相关设备** | 主服务器、控制面处理器、管理控制器、实验室交换机 |
| **目标** | 梳理网络排障、整机固件升级和通信验证的一般方法 |

> 本文已移除现场人员、资产标识、地址、端口规划和设备级操作命令。实际操作必须遵循所在环境的变更流程，并以厂商文档为准。

---

## 一、背景知识：接口与部件

### 1.1 服务器后面板接口

服务器后面板通常同时提供业务网口、管理网口、串口和扩展网口：

| 接口 | 类型 | 说明 |
| :--- | :--- | :--- |
| 主机业务网口 | 网口 | 主服务器 OS 业务网 |
| 控制面业务网口 | 网口 | 控制面处理器业务网 |
| 管理网口 | 网口 | 带外管理网络 |
| 控制面串口 | 串口 | 控制面 Console |
| 管理控制器串口 | 串口 | 管理控制器调试 |
| OCP 扩展网口 | 网口 | 扩展用 |

**网口与串口的分工**：业务数据走网口；串口不依赖 IP 配置，只用于获得授权的应急诊断和初始配置。串口通常拥有较高权限，应限制物理接入并记录操作审计。

> 原文包含服务器实体和接口照片；公开版未附带现场图片。

### 1.2 BMC

BMC 是独立于主机 OS 的管理控制器，常见管理方式包括：

- **受控管理界面**：查看服务器状态、执行批准的维护操作和收集日志；
- **受控命令行**：供高级诊断使用，应通过最小权限和审计策略限制访问。

### 1.3 控制面处理器

部分加速服务器带有独立的**控制面处理器**，负责加速卡注册、上线状态和控制面软件维护。控制面与主机可能使用不同的网络接口和地址配置，必须分别依据批准的网络方案核对。

### 1.4 三层管控关系

主机、管理控制器和控制面处理器职责不同：

```text
管理控制器（带外，独立于主机 OS）
 └── 电源、硬件健康、事件日志和受控带外升级

控制面处理器
 └── 加速卡注册、上线状态和控制面软件维护

主机 OS（带内）
 └── 业务、驱动、带内升级和运行日志
```

**选择依据**：优先使用权限最低且影响最小的入口。只有主机不可用或确需维护底层部件时，才在已审批的维护窗口使用带外通道。

---

## 二、网络架构

### 2.1 网络分区

加速服务器环境通常至少包含三类逻辑网络：

| 网络 | 用途 | 常见接口 | 安全要求 |
| :--- | :--- | :--- | :--- |
| 管理网络 | 带外管理、救砖、固件维护 | 管理网口 | 与业务网隔离，仅授权运维访问 |
| 业务网络 | 主机和控制面处理器的正常通信 | 业务网口 | 按业务访问控制策略开放 |
| 部署网络 | PXE、装机或临时维护 | 主机业务网口 | 仅在部署窗口临时接入 |

不同网络通常通过 VLAN、物理端口或访问控制策略隔离。接口接到错误的网络时，即使网口灯亮，也可能无法解析网关或访问目标服务。

### 2.2 接口核对方法

现场操作前应从资产台账或经批准的网络规划中确认以下映射，不要依赖接口外观猜测：

| 核对项 | 需要确认的内容 |
| :--- | :--- |
| 接口身份 | 主机业务口、控制面业务口、管理口或串口 |
| 网络用途 | 管理、业务或部署网络 |
| 交换机配置 | VLAN、端口状态、速率和链路聚合配置 |
| 地址配置 | 地址、掩码、网关和 DNS 是否来自同一批准方案 |

> 原文包含交换机现场照片；公开版未附带现场图片。

### 2.3 串口的使用边界

串口不依赖 IP 配置，适合在网络不可用时进行受控诊断。远程串口属于高权限管理通道，应限制在专用管理网络，启用访问审计，并只向获得授权的运维人员开放。公开文档不记录具体入口、账号或切换命令。

---

## 三、网络排障方法

### 3.1 常见现象

- 邻居表中网关条目长期显示 `<incomplete>`；
- 网关连通性测试全部超时；
- 远程管理或业务连接超时；
- 网口灯亮，但业务网络仍不可达。

> `<incomplete>` 表示邻居解析请求没有得到应答，通常应优先检查物理链路、VLAN、端口安全策略和二层网络，而不是直接把问题归因于上层服务。

### 3.2 分层排查

**阶段 1：物理层和链路层**

1. 依据资产台账确认设备、线缆和交换机端口的对应关系。
2. 区分网线与串口线，确认线缆类型和收发模块符合要求。
3. 检查两端链路指示灯、协商速率和端口错误计数。
4. 请网络管理员确认端口已启用，并核对 VLAN、端口安全和链路聚合配置。
5. 确认业务接口没有误接入管理网络或部署网络。

**阶段 2：网络层**

1. 使用 `ip addr` 和 `ip route` 查看接口、地址和路由状态。
2. 对照批准的网络方案核对地址、前缀长度和默认网关；不要把现场地址写入公开文档。
3. 使用 `ip neigh` 或 `arp -n` 检查网关的邻居解析是否完成。
4. 在变更窗口内按“本机接口 → 同网段网关 → 目标服务”的顺序验证连通性。

地址、前缀和网关必须自洽：默认网关应位于接口可直达的子网内。若不满足这一点，即使链路正常，主机也无法直接解析网关的链路层地址。

**阶段 3：服务验证**

在网络连通后，再使用经过批准的非特权账号验证远程访问或业务端口。连续执行多轮短连接和稳定性检查，确认不存在偶发丢包、路由漂移或会话中断。

### 3.3 根因模式

一个“网络不通”现象可能同时包含多层原因：

| 现象 | 常见原因 | 处理原则 |
| :--- | :--- | :--- |
| 网口灯不亮 | 线缆、模块、端口状态或物理链路异常 | 先恢复物理链路，再继续上层检查 |
| 网口灯亮但网关不可达 | VLAN/网络用途错误，或地址与网关不自洽 | 核对端口网络属性和批准的地址方案 |
| 网关可达但服务不可用 | 路由、访问控制、主机防火墙或服务状态异常 | 沿数据路径逐跳验证 |

### 3.4 经验沉淀

排障应按层进行并逐层确认：

| 层 | 看什么 | 通过的标志 |
| :--- | :--- | :--- |
| 物理层 | 线缆、模块、端口和协商状态 | 链路稳定、无异常错误计数 |
| 链路层 | VLAN、邻居解析、端口安全策略 | 网关邻居项正常解析 |
| 网络层 | 地址、路由和网关连通性 | 到网关和目标网段的路径正常 |
| 应用层 | 授权访问或业务健康检查 | 服务连接稳定且行为符合预期 |

**链路灯亮只证明物理链路建立，不代表接口进入了正确的逻辑网络。** 物理链路和网络分区必须同时正确，业务网络才可用。

---

## 四、服务器固件升级

服务器升级可能覆盖：**管理控制器、BIOS、CPLD、OS 内核、设备固件、驱动和通信配置**。具体组件、顺序和兼容矩阵必须以厂商发布说明为准。

### 4.1 各部件说明

| 部件 | 说明 |
| :--- | :--- |
| **CSP** | 提供硬件信息、器件互联关系及能力描述 |
| **CPLD** | 复杂可编程逻辑器件 |
| **固件** | 分带内 / 带外两种升级方式，见 4.2 |
| **控制面软件包** | 与控制面和通信能力相关，需和整机版本配套 |
| **通信拓扑配置** | 与机型和物理拓扑强相关 |

> **"与机型强相关"意味着**：换机型 / 换拓扑必须同步换配置。沿用旧机型的配置往往不会报
> "配置错误"，而是表现为通信建链失败或打流跑不起来——症状离根因很远，值得单独记一笔。

### 4.2 固件的两种升级方式

| 方式 | 怎么做 | 前提 | 适用 |
| :--- | :--- | :--- | :--- |
| **带内** | 通过主机 OS 和厂商升级工具执行 | OS 正常且版本满足要求 | 常规驱动和固件升级 |
| **带外** | 通过受控管理通道和厂商维护流程执行 | 已审批维护窗口，设备已排空并可下电 | OS 不可用或需要升级底层部件 |

**本质区别**：带内依赖主机 OS，带外不依赖。带外是主机不可用时的兜底路径，但权限更高、影响更大，必须执行双人复核、访问审计和回滚准备。

---

## 五、调试与日志收集

按“从哪一层看问题”选择日志入口，不要一开始就进入高权限管理通道。

### 5.1 管理控制器

优先使用厂商支持的日志收集功能，记录升级任务状态、硬件告警和事件日志。导出前检查日志是否含资产编号、地址、账号、序列号或客户数据，并按公开范围脱敏。

### 5.2 控制面处理器

重点检查加速卡注册状态、上线状态和控制面软件版本。后台诊断入口属于高权限接口，具体进入方法和命令只应保存在受控运维手册中。

### 5.3 设备串口

设备串口适合在网络或主机 OS 不可用时诊断底层问题。操作前必须确认模块与端口的拓扑关系，并由有权限的人员按照厂商指导执行。

公开文档不提供抽屉切换、模组切换、寄存器读写或串口激活命令。这些命令与具体硬件版本强相关，误用可能导致设备失联、数据不一致或不可恢复的停机。

### 5.4 主机 OS

| 手段 | 说明 |
| :--- | :--- |
| 内核日志 | 查看驱动加载、链路变化和硬件错误的首条异常 |
| 运行日志 | 关联业务时间线，区分首错和连锁错误 |
| 厂商日志包 | 收集驱动、固件和设备侧诊断信息 |

### 5.5 通信栈

从主机和设备两侧收集通信建链、拓扑和错误统计。寄存器裸值必须由熟悉对应硬件版本的人员解释；不要在公开文档中发布物理地址、寄存器地址或直接内存访问命令。

---

## 六、打流验证

### 6.1 通信库验证

1. 根据整机兼容矩阵安装匹配版本的 MPI、驱动、固件和通信库。
2. 固定拓扑、设备数、数据类型、消息大小和测试轮数，记录完整版本信息。
3. 使用厂商提供的正式测试工具，先做小规模连通性验证，再逐步扩大到目标拓扑。
4. 同时记录带宽、时延、错误计数和退出状态；性能通过但有错误计数仍应视为失败。
5. 测试完成后恢复临时环境变量和网络配置，保留可审计的结果摘要。

### 6.2 设备间传输验证

1. 在维护窗口内确认参与设备均已排空，拓扑和链路状态符合预期。
2. 使用经过批准的工具分别验证主机到设备、设备到主机和设备间传输。
3. 不要通过破解、共享密码或永久免密方式建立测试通道；使用短期凭据和最小权限账号。
4. 发生链路错误时，先保存日志和现场状态，再按变更流程回滚。

---

## 七、升级与问题升级机制

日志中往往有大量连锁错误，应先按时间顺序定位**首个异常模块**，再交给对应职责团队处理。公开文档只保留角色，不记录个人联系方式。

| 问题方向 | 首要负责角色 | 建议提供的材料 |
| :--- | :--- | :--- |
| 整机或拓扑 | 系统集成团队 | 拓扑摘要、版本矩阵、复现步骤 |
| 驱动或固件 | 驱动/固件团队 | 内核日志、固件版本、厂商日志包 |
| 通信库 | 通信软件团队 | 建链日志、测试参数、错误计数 |
| 物理链路 | 硬件与网络团队 | 链路状态、端口统计、线缆和模块信息 |
| 控制面 | 平台运维团队 | 控制面版本、设备注册状态、事件时间线 |

升级问题时应附上：影响范围、首错时间、最近变更、可重复性、已执行检查和回滚状态。人员通讯录应维护在受访问控制的内部系统中，不应提交到公开代码仓库。
